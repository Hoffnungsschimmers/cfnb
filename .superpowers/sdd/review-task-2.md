# Review package Task 2

Commits:


Stat:
 lib/core/config/config_repository.dart | 63 ++++++++++++++++++++++++++++------  1 file changed, 53 insertions(+), 10 deletions(-)

## Full diff

``diff
diff --git a/lib/core/config/config_repository.dart b/lib/core/config/config_repository.dart index c64311b..9c3693d 100644 --- a/lib/core/config/config_repository.dart +++ b/lib/core/config/config_repository.dart @@ -1,41 +1,84 @@  import 'dart:convert';    import 'package:shared_preferences/shared_preferences.dart';    import 'app_config.dart';   -/// 閰嶇疆鎸佷箙鍖栦粨搴撱€?-/// -/// - 鍐呭瓨涓寔鏈夊綋鍓?[AppConfig]锛堝崟渚嬭涔夛級銆?-/// - 鍚姩鏃跺皾璇曚粠搴旂敤绉佹湁鐩綍鐨?config.json 璇诲彇锛涗笉瀛樺湪鍒欎娇鐢ㄩ粯璁ゅ€笺€?-/// - 淇濆瓨鏃跺啓鍥?shared_preferences锛堣交閲忓瓧娈碉級涓庡簲鐢ㄧ洰褰?config.json锛堝畬鏁达級銆?+/// 閰嶇疆鎸佷箙鍖栦粨搴撱€傚唴瀛樻寔鏈夊綋鍓?[AppConfig]锛堝崟渚嬭涔夛級銆?+/// 鍚姩鏃朵粠 shared_preferences 璇诲彇锛涗笉瀛樺湪鍒欑敤榛樿鍊笺€傛棫閿拷鐣ワ紝鍚戝悗鍏煎銆? class ConfigRepository {    AppConfig _config;    final SharedPreferences _prefs;      ConfigRepository._(this._config, this._prefs);      static Future<ConfigRepository> init() async {      final prefs = await SharedPreferences.getInstance();      final jsonStr = prefs.getString(_kConfigJson); -    final config = jsonStr != null -        ? AppConfig.fromJson(jsonDecode(jsonStr) as Map<String, dynamic>) -        : const AppConfig(); +    final Map<String, dynamic> data = jsonStr != null +        ? (jsonDecode(jsonStr) as Map<String, dynamic>) +        : <String, dynamic>{}; +    var config = data.isNotEmpty ? AppConfig.fromJson(data) : const AppConfig(); +    var dirty = false; + +    // 杩佺Щ锛氭棫閰嶇疆鑻ユ病鏈夋暟鎹簮锛岃ˉ鍏ラ粯璁ゆ簮銆?+    if (config.additionalSources.isEmpty) { +      config = config.copyWith( +        additionalSources: defaultAdditionalSources, +        subGenerators: defaultSubGenerators, +      ); +      dirty = true; +    } + +    // 杩佺Щ锛氭棫榛樿鍥藉 UN 瑙嗕负鏈厤缃紝鏀逛负绌轰覆锛堢敱鑺傜偣鍚嶅浗瀹剁爜鍐冲畾锛夈€?+    if (config.subDefaultCountry.toUpperCase() == 'UN') { +      config = config.copyWith(subDefaultCountry: ''); +      dirty = true; +    } + +    // 杩佺Щ锛氬甫瀹芥祴閫熷弬鏁板崌绾т负鏇寸湡瀹炵殑榛樿鍊硷紙浠呭綋鐢ㄦ埛浠嶇敤鏃ч粯璁ゅ€兼椂鐢熸晥锛夈€?+    if (config.subSpeedSizeMb == 1.0) { +      config = config.copyWith(subSpeedSizeMb: 10.0); +      dirty = true; +    } +    if (config.subSpeedTimeout == 15.0) { +      config = config.copyWith(subSpeedTimeout: 20.0); +      dirty = true; +    } +    if (config.subSpeedWorkers == 20) { +      config = config.copyWith(subSpeedWorkers: 10); +      dirty = true; +    } +    if (config.subLatencyTimeout == 2.0) { +      config = config.copyWith(subLatencyTimeout: 3.0); +      dirty = true; +    } +    if (config.subLatencyMaxMs == 0) { +      config = config.copyWith(subLatencyMaxMs: 200); +      dirty = true; +    } +    if (config.subSpeedLatencyLimit == 0) { +      config = config.copyWith(subSpeedLatencyLimit: 200); +      dirty = true; +    } + +    if (dirty) { +      await prefs.setString(_kConfigJson, jsonEncode(config.toJson())); +    } +      return ConfigRepository._(config, prefs);    }      static const _kConfigJson = 'app_config_json';      AppConfig get current => _config;   -  /// 鐢ㄤ竴浠藉畬鏁撮厤缃浛鎹㈠苟鎸佷箙鍖栥€?   Future<void> save(AppConfig config) async {      _config = config;      await _prefs.setString(_kConfigJson, jsonEncode(config.toJson()));    }   -  /// 灞€閮ㄦ洿鏂帮紙浼犲叆淇敼鍚庣殑涓€浠介厤缃級銆?   Future<void> update(AppConfig config) => save(config);      List<String> validateCurrent() => _config.validate();  }
``
