# Review package Task 6

Commits:


Stat:
 lib/features/results/result_state.dart | 161 +++++++++++++++++++++++++++++++++  test/features/result_state_test.dart   |  23 +++++  2 files changed, 184 insertions(+)

## Full diff

``diff
diff --git a/lib/features/results/result_state.dart b/lib/features/results/result_state.dart new file mode 100644 index 0000000..6792cba --- /dev/null +++ b/lib/features/results/result_state.dart @@ -0,0 +1,161 @@ +import 'dart:io'; + +import 'package:cfnb_app/core/latency/latency_prober.dart'; +import 'package:flutter_riverpod/flutter_riverpod.dart'; + +/// 缁撴灉琛岋紙瀵瑰簲鏃х増 ResultsPanel 鐨勮〃鏍艰锛夈€? +class ResultRow { +  final String node; // ip:port#CC +  final String? speed; // "12.34 Mbps" 鎴?null +  final String? latency; // "50.00 ms" 鎴?null +  final double? quality; // 缁煎悎璐ㄩ噺鍒?Q锛?-1锛夛紝鏃犲垯 null +  ResultRow(this.node, [this.speed, this.latency, this.quality]); + +  String get ipPort => node.split('#').first; +  String get country => nodeCountry(node); +} + +/// 瑙ｆ瀽 ip.txt / addressesapi*.txt 鍐呭涓虹粨鏋滆銆? +/// 鍏煎涓ょ鏍煎紡锛? +///   - 甯︽祴閫燂細"1.2.3.4:443#US 12.34 Mbps 50.00 ms" +///   - 绾妭鐐癸細"1.2.3.4:443#US" +List<ResultRow> parseResultLines(String text) { +  final rows = <ResultRow>[]; +  for (final raw in text.split('\n')) { +    final line = raw.trim(); +    if (line.isEmpty || line.startsWith('#')) continue; +    final parts = line.split(RegExp(r'\s+')); +    final node = parts.first; +    if (!node.contains(':')) continue; +    String? speed; +    String? latency; +    double? quality; +    if (parts.length > 1) { +      // 鍓╀綑鐗囨涓瘑鍒?"120.50 Mbps" / "30.10 ms"锛堝€?鍗曚綅锛? +      final tokens = parts.skip(1).toList(); +      for (var i = 0; i < tokens.length; i++) { +        final p = tokens[i]; +        if (p.contains('Mbps')) speed ??= (i > 0 ? '${tokens[i - 1]} $p' : p); +        if (p.contains('ms')) latency ??= (i > 0 ? '${tokens[i - 1]} $p' : p); +        if (p.startsWith('Q')) { +          final q = double.tryParse(p.substring(1)); +          quality ??= q; +        } +      } +    } +    rows.add(ResultRow(node, speed, latency, quality)); +  } +  return rows; +} + +class ResultState { +  final List<ResultRow> rows; +  final String? sourceLabel; +  final String? currentFile; +  final String? rawText; +  ResultState({this.rows = const [], this.sourceLabel, this.currentFile, this.rawText}); +  ResultState copyWith({ +    List<ResultRow>? rows, +    String? sourceLabel, +    String? currentFile, +    String? rawText, +    bool clearFile = false, +  }) => +      ResultState( +        rows: rows ?? this.rows, +        sourceLabel: sourceLabel ?? this.sourceLabel, +        currentFile: clearFile ? null : (currentFile ?? this.currentFile), +        rawText: clearFile ? null : (rawText ?? this.rawText), +      ); +} + +class ResultNotifier extends StateNotifier<ResultState> { +  ResultNotifier() : super(ResultState()); + +  void setRows(List<ResultRow> rows, [String? sourceLabel]) { +    state = state.copyWith(rows: rows, sourceLabel: sourceLabel); +  } + +  /// 浠庢枃浠跺姞杞斤紙鐢ㄤ簬銆屽埛鏂般€嶄笌鍗曟鎵ц鍚庯級銆? +  /// 鍚屾椂淇濆瓨鍘熷鏂囨湰锛屼緵銆屾煡鐪嬫枃浠跺唴瀹广€嶉潰鏉夸娇鐢ㄣ€? +  Future<void> loadFile(String path) async { +    final f = File(path); +    if (!f.existsSync()) { +      state = state.copyWith(clearFile: true); +      return; +    } +    final text = await f.readAsString(); +    state = state.copyWith( +      rows: parseResultLines(text), +      sourceLabel: path.split(RegExp(r'[\\/]')).last, +      currentFile: path, +      rawText: text, +    ); +  } + +  /// 浠呭姞杞芥煇鏂囦欢鐨勫師濮嬪唴瀹癸紙涓嶈В鏋愪负鑺傜偣琛級锛岀敤浜庢煡鐪?work/ 涓棿浜х墿銆? +  Future<void> loadRaw(String path) async { +    final f = File(path); +    if (!f.existsSync()) { +      state = state.copyWith(clearFile: true); +      return; +    } +    final text = await f.readAsString(); +    state = state.copyWith( +      rows: parseResultLines(text), +      sourceLabel: path.split(RegExp(r'[\\/]')).last, +      currentFile: path, +      rawText: text, +    ); +  } + +  /// 浠庤妭鐐硅鍒楄〃鍔犺浇锛堝崟姝ワ細鑾峰彇鏁版嵁婧?/ 鍙敤鎬х粨鏋滐級銆? +  void loadLines(List<String> lines, [String? label]) { +    final rows = lines +        .where((l) => l.trim().isNotEmpty && !l.trim().startsWith('#')) +        .map((l) => ResultRow(l.trim())) +        .toList(); +    state = state.copyWith(rows: rows, sourceLabel: label); +  } + +  /// 浠?"鑺傜偣 -> 鏁板€? 鏄犲皠鍔犺浇锛堝崟姝ワ細TCP 寤惰繜 / 甯﹀娴嬮€熺粨鏋滐級銆? +  void loadMap(Map<String, double> map, String label) { +    final rows = map.entries +        .map((e) => ResultRow(e.key, e.value.toStringAsFixed(2))) +        .toList(); +    state = state.copyWith(rows: rows, sourceLabel: label); +  } + +  Map<String, int> geoDistribution() { +    final map = <String, int>{}; +    for (final r in state.rows) { +      final c = r.country.isEmpty ? '鏈煡' : r.country; +      map[c] = (map[c] ?? 0) + 1; +    } +    return map; +  } + +  double? bestSpeed() { +    double? best; +    for (final r in state.rows) { +      if (r.speed == null) continue; +      final v = double.tryParse(r.speed!.replaceAll(RegExp(r'[^0-9.]'), '')); +      if (v != null && (best == null || v > best)) best = v; +    } +    return best; +  } + +  double? lowestLatency() { +    double? low; +    for (final r in state.rows) { +      if (r.latency == null) continue; +      final v = double.tryParse(r.latency!.replaceAll(RegExp(r'[^0-9.]'), '')); +      if (v != null && (low == null || v < low)) low = v; +    } +    return low; +  } +} + +final resultProvider = StateNotifierProvider<ResultNotifier, ResultState>( +  (ref) => ResultNotifier(), +); diff --git a/test/features/result_state_test.dart b/test/features/result_state_test.dart new file mode 100644 index 0000000..6bca016 --- /dev/null +++ b/test/features/result_state_test.dart @@ -0,0 +1,23 @@ +import 'package:cfnb_app/features/results/result_state.dart'; +import 'package:flutter_test/flutter_test.dart'; + +void main() { +  group('ResultRow.country', () { +    test('uses nodeCountry (splits at @)', () { +      expect(ResultRow('1.2.3.4:443#US@CM').country, 'US'); +      expect(ResultRow('1.2.3.4:443#JP').country, 'JP'); +      expect(ResultRow('1.2.3.4:443').country, ''); +    }); +  }); +  group('parseResultLines Q', () { +    test('parses quality score', () { +      final rows = parseResultLines('1.2.3.4:443#US 12.34Mbps 50.00ms Q0.87'); +      expect(rows.length, 1); +      expect(rows.first.quality, 0.87); +    }); +    test('quality null when absent', () { +      final rows = parseResultLines('1.2.3.4:443#US 12.34Mbps 50.00ms'); +      expect(rows.first.quality, isNull); +    }); +  }); +}
``
