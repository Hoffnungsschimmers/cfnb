# Review package Task 9

Commits:


Stat:
 lib/features/settings/settings_fields.dart |  66 +++++++++  lib/features/settings/settings_page.dart   | 206 ++++++++++++++++-------------  test/features/settings_results_test.dart   |  61 +++++++++  3 files changed, 242 insertions(+), 91 deletions(-)

## Full diff

``diff
diff --git a/lib/features/settings/settings_fields.dart b/lib/features/settings/settings_fields.dart new file mode 100644 index 0000000..3908bae --- /dev/null +++ b/lib/features/settings/settings_fields.dart @@ -0,0 +1,66 @@ +/// 璁剧疆瀛楁澹版槑锛堟暟鎹┍鍔ㄦ覆鏌擄紝瀵瑰簲鏃х増 gui/constants.py 鐨?SETTINGS_FIELDS锛夈€?+/// +/// 绫诲瀷锛歜ool / int / float / string / list_int / list_str / choice / +///       sources锛堣妭鐐规簮澶氳 URL锛? multiline锛堝琛屾枃鏈級銆?+/// opts锛歩nt/float 涓?(min,max,step)锛沜hoice 涓哄彲閫夊€煎垪琛紱鍏朵綑涓?null銆?+class SettingField { +  final String category; +  final String label; +  final String key; +  final String type; +  final List<dynamic>? opts; +  const SettingField(this.category, this.label, this.key, this.type, [this.opts]); +} + +const List<SettingField> settingsFields = [ +  // 1. 璁㈤槄杞崲 +  SettingField('1. 璁㈤槄杞崲', '涓绘祦绋嬭嚜鍔?, 'SUB_CONVERT_ENABLED', 'bool'), +  SettingField('1. 璁㈤槄杞崲', '杈撳叆妯″紡', 'SUB_INPUT_MODE', 'choice', ['both', 'node', 'url']), +  SettingField('1. 璁㈤槄杞崲', '鍊欓€夎闃呭櫒', 'SUB_GENERATORS', 'list_str'), +  SettingField('1. 璁㈤槄杞崲', '鑺傜偣鍩熷悕', 'SUB_NODE_HOST', 'string'), +  SettingField('1. 璁㈤槄杞崲', '鑺傜偣UUID', 'SUB_NODE_UUID', 'string'), +  SettingField('1. 璁㈤槄杞崲', '璁㈤槄閾炬帴', 'SUB_URLS', 'list_str'), +  SettingField('1. 璁㈤槄杞崲', '杈撳嚭鏂囦欢', 'SUB_OUTPUT_FILE', 'string'), +  SettingField('1. 璁㈤槄杞崲', '榛樿鍥藉', 'SUB_DEFAULT_COUNTRY', 'string'), +  SettingField('1. 璁㈤槄杞崲', '鍩熷悕瑙ｆ瀽IP', 'SUB_RESOLVE_DOMAIN', 'bool'), + +  // 2. 寤惰繜浼橀€?+  SettingField('2. 寤惰繜浼橀€?, '寤惰繜浣庝簬(ms)', 'SUB_LATENCY_MAX_MS', 'int', [10, 1000, 1]), +  SettingField('2. 寤惰繜浼橀€?, '鎺㈡祴娆℃暟', 'SUB_LATENCY_PROBES', 'int', [1, 10, 1]), +  SettingField('2. 寤惰繜浼橀€?, '杈撳嚭鏂囦欢', 'SUB_LATENCY_OUTPUT_FILE', 'string'), +  SettingField('2. 寤惰繜浼橀€?, '杩炴帴瓒呮椂(绉?', 'SUB_LATENCY_TIMEOUT', 'float', [0.1, 30.0, 0.1]), +  SettingField('2. 寤惰繜浼橀€?, '骞跺彂鏁?, 'SUB_LATENCY_WORKERS', 'int', [1, 1000, 1]), +  SettingField('2. 寤惰繜浼橀€?, 'SNI', 'SUB_LATENCY_SNI', 'string'), +  SettingField('2. 寤惰繜浼橀€?, '璐ㄩ噺寤惰繜鏉冮噸', 'SUB_QUALITY_LATENCY_WEIGHT', 'float', [0.0, 1.0, 0.05]), + +  // 3. 甯﹀娴嬮€?+  SettingField('3. 甯﹀娴嬮€?, '鍚敤', 'SUB_SPEED_ENABLED', 'bool'), +  SettingField('3. 甯﹀娴嬮€?, '浠呮祴寤惰繜鈮?ms)', 'SUB_SPEED_LATENCY_LIMIT', 'int', [50, 500, 1]), +  SettingField('3. 甯﹀娴嬮€?, '瓒呮椂(绉?', 'SUB_SPEED_TIMEOUT', 'float', [1, 60, 1]), +  SettingField('3. 甯﹀娴嬮€?, '骞跺彂', 'SUB_SPEED_WORKERS', 'int', [1, 100, 1]), +  SettingField('3. 甯﹀娴嬮€?, '涓嬭浇(MB)', 'SUB_SPEED_SIZE_MB', 'float', [0.1, 10, 0.1]), + +  // 4. GitHub 鎺ㄩ€?+  SettingField('4. GitHub 鎺ㄩ€?, '浠撳簱', 'GITHUB_REPO', 'string'), +  SettingField('4. GitHub 鎺ㄩ€?, '鍒嗘敮', 'GITHUB_BRANCH', 'string'), + +  // 5. 澶栬 +  SettingField('5. 澶栬', '涓婚', 'GUI_THEME', 'choice', ['light', 'dark']), +]; + +/// 鎶婅崏绋?JSON 涓殑鐗规畩瀛楁锛坰ources锛夎浆鎹负 AppConfig 鑳借瘑鍒殑缁撴瀯銆?+Map<String, dynamic> normalizeDraft(Map<String, dynamic> draft) { +  final out = <String, dynamic>{...draft}; +  // SUB_GENERATORS / SUB_URLS / ALLOWED_COUNTRIES 绛変负 list_str锛屼繚鎸?list +  // ADDITIONAL_SOURCES 鍦?UI 涓互澶氳鏂囨湰缂栬緫锛屼繚瀛樻椂杞洖 [{url,enabled}] +  if (out['ADDITIONAL_SOURCES'] is String) { +    final lines = (out['ADDITIONAL_SOURCES'] as String) +        .split('\n') +        .map((e) => e.trim()) +        .where((e) => e.isNotEmpty) +        .map((e) => {'url': e, 'enabled': true}) +        .toList(); +    out['ADDITIONAL_SOURCES'] = lines; +  } +  return out; +} diff --git a/lib/features/settings/settings_page.dart b/lib/features/settings/settings_page.dart index c1d5652..53d2f3e 100644 --- a/lib/features/settings/settings_page.dart +++ b/lib/features/settings/settings_page.dart @@ -1,91 +1,115 @@ -import 'package:flutter/material.dart'; -import 'package:flutter_riverpod/flutter_riverpod.dart'; - -import '../../app/theme.dart'; -import '../../core/config/app_config.dart'; -import '../widgets/common.dart'; -import '../../app/providers.dart'; - -class SettingsPage extends ConsumerStatefulWidget { -  const SettingsPage({super.key}); -  @override -  ConsumerState<SettingsPage> createState() => _SettingsPageState(); -} - -class _SettingsPageState extends ConsumerState<SettingsPage> { -  final _scroll = ScrollController(); - -  @override -  Widget build(BuildContext context) { -    final cfgAsync = ref.watch(configProvider); -    return cfgAsync.when( -      loading: () => const Center(child: CircularProgressIndicator()), -      error: (e, _) => Center(child: Text('鍔犺浇閰嶇疆澶辫触: $e')), -      data: (cfg) => SingleChildScrollView( -        controller: _scroll, -        padding: const EdgeInsets.all(20), -        child: Column( -          crossAxisAlignment: CrossAxisAlignment.start, -          children: [ -            sectionTitle(context, '鍩烘湰璁剧疆'), -            const SizedBox(height: 12), -            card(context, child: _switchRow(context, '鍚敤鍏ㄥ眬妯″紡', cfg.useGlobalMode, -                (v) => _save(cfg.copyWith(useGlobalMode: v)))), -            card(context, child: _sliderRow(context, '淇濈暀鑺傜偣鏁?(TOP N)', cfg.globalTopN.toDouble(), 1, 200, -                (v) => _save(cfg.copyWith(globalTopN: v.round())))), -            card(context, child: _sliderRow(context, '甯﹀娴嬮€熷苟鍙?, cfg.bandwidthWorkers.toDouble(), 1, 200, -                (v) => _save(cfg.copyWith(bandwidthWorkers: v.round())))), -            card(context, child: _switchRow(context, '鍚敤 Cloudflare DNS 鏇存柊', cfg.cfEnabled, -                (v) => _save(cfg.copyWith(cfEnabled: v)))), -            card(context, child: _switchRow(context, '鍚敤璁㈤槄杞崲', cfg.subConvertEnabled, -                (v) => _save(cfg.copyWith(subConvertEnabled: v)))), -            card(context, child: _switchRow(context, '鍚敤鑷姩璋冨害', cfg.autoScheduleEnabled, -                (v) => _save(cfg.copyWith(autoScheduleEnabled: v)))), -            const SizedBox(height: 12), -            AppButton('淇濆瓨璁剧疆', icon: Icons.save, onPressed: () { -              ScaffoldMessenger.of(context).showSnackBar( -                const SnackBar(content: Text('璁剧疆宸蹭繚瀛?)), -              ); -            }), -          ], -        ), -      ), -    ); -  } - -  void _save(AppConfig next) async { -    final repo = await ref.read(configRepositoryProvider.future); -    await repo.save(next); -    ref.invalidate(configProvider); -  } -} - -Widget _switchRow(BuildContext context, String label, bool value, ValueChanged<bool> onChanged) { -  final t = AppThemeExt.of(context); -  return Row( -    children: [ -      Expanded(child: Text(label, style: TextStyle(color: t.text))), -      Switch(value: value, activeColor: AppTheme.edgeOrange, onChanged: onChanged), -    ], -  ); -} - -Widget _sliderRow(BuildContext context, String label, double value, double min, double max, -    ValueChanged<double> onChanged) { -  final t = AppThemeExt.of(context); -  return Column( -    crossAxisAlignment: CrossAxisAlignment.start, -    children: [ -      Row( -        children: [ -          Expanded(child: Text(label, style: TextStyle(color: t.text))), -          Text(value.round().toString(), -              style: const TextStyle(fontWeight: FontWeight.bold, color: AppTheme.edgeOrange)), -        ], -      ), -      Slider(value: value, min: min, max: max, activeColor: AppTheme.edgeOrange, onChanged: onChanged), -    ], -  ); -} - - +import 'package:flutter/material.dart'; +import 'package:flutter_riverpod/flutter_riverpod.dart'; + +import '../../app/theme.dart'; +import '../../core/config/app_config.dart'; +import '../widgets/common.dart'; +import '../../app/providers.dart'; + +class SettingsPage extends ConsumerStatefulWidget { +  const SettingsPage({super.key}); +  @override +  ConsumerState<SettingsPage> createState() => _SettingsPageState(); +} + +class _SettingsPageState extends ConsumerState<SettingsPage> { +  final Map<String, TextEditingController> _ctl = {}; + +  TextEditingController _c(String key, [String initial = '']) => +      _ctl.putIfAbsent(key, () => TextEditingController(text: initial)); + +  @override +  void dispose() { +    for (final c in _ctl.values) { c.dispose(); } +    super.dispose(); +  } + +  Future<void> _save(AppConfig next) async { +    final repo = await ref.read(configRepositoryProvider.future); +    await repo.save(next); +    ref.invalidate(configProvider); +  } + +  @override +  Widget build(BuildContext context) { +    final cfgAsync = ref.watch(configProvider); +    return cfgAsync.when( +      loading: () => const Center(child: CircularProgressIndicator()), +      error: (e, _) => Center(child: Text('鍔犺浇閰嶇疆澶辫触: $e')), +      data: (cfg) { +        // 鏂囨湰鎺у埗鍣細浠呭湪棣栨鎴栧閮ㄥ€煎彉鍖栨椂鍚屾 +        void sync(String k, String v) { +          final c = _ctl[k]; +          if (c != null && c.text != v) c.text = v; +        } + +        return SingleChildScrollView( +          padding: const EdgeInsets.all(20), +          child: Column( +            crossAxisAlignment: CrossAxisAlignment.start, +            children: [ +              sectionTitle(context, 'GitHub 鎺ㄩ€?(鐙珛 cf-ip 浠?'), +              const SizedBox(height: 12), +              card(context, child: _text(context, 'Token', cfg.githubToken, sync, 'githubToken', +                  (v) => _save(cfg.copyWith(githubToken: v)), obscure: true)), +              card(context, child: _text(context, '浠撳簱 (owner/repo)', cfg.githubRepo, sync, 'githubRepo', +                  (v) => _save(cfg.copyWith(githubRepo: v)))), +              card(context, child: _text(context, '鍒嗘敮', cfg.githubBranch, sync, 'githubBranch', +                  (v) => _save(cfg.copyWith(githubBranch: v)))), +              const SizedBox(height: 16), + +              sectionTitle(context, '璁㈤槄杞崲'), +              const SizedBox(height: 12), +              card(context, child: _switch(context, '鍚敤璁㈤槄杞崲', cfg.subConvertEnabled, +                  (v) => _save(cfg.copyWith(subConvertEnabled: v)))), +              const SizedBox(height: 12), + +              AppButton('淇濆瓨璁剧疆', icon: Icons.save, onPressed: () { +                ScaffoldMessenger.of(context).showSnackBar( +                  const SnackBar(content: Text('璁剧疆宸蹭繚瀛?)), +                ); +              }), +            ], +          ), +        ); +      }, +    ); +  } + +  Widget _switch(BuildContext context, String label, bool value, ValueChanged<bool> onChanged) { +    final t = AppThemeExt.of(context); +    return Row( +      children: [ +        Expanded(child: Text(label, style: TextStyle(color: t.text))), +        Switch(value: value, activeThumbColor: AppTheme.edgeOrange, onChanged: onChanged), +      ], +    ); +  } + +  Widget _text(BuildContext context, String label, String value, void Function(String, String) sync, String key, +      ValueChanged<String> onChanged, {bool obscure = false}) { +    sync(key, value); +    final t = AppThemeExt.of(context); +    return Column( +      crossAxisAlignment: CrossAxisAlignment.start, +      children: [ +        Text(label, style: TextStyle(fontSize: 12, color: t.textDim)), +        const SizedBox(height: 4), +        TextField( +          controller: _c(key, value), +          onChanged: onChanged, +          obscureText: obscure, +          style: const TextStyle(fontFamily: 'Consolas', fontSize: 13), +          decoration: InputDecoration( +            isDense: true, +            filled: true, +            fillColor: t.bg, +            border: OutlineInputBorder(borderRadius: t.radius, borderSide: BorderSide(color: t.border)), +            contentPadding: const EdgeInsets.symmetric(horizontal: 10, vertical: 8), +          ), +        ), +      ], +    ); +  } + +} diff --git a/test/features/settings_results_test.dart b/test/features/settings_results_test.dart new file mode 100644 index 0000000..8bea3f8 --- /dev/null +++ b/test/features/settings_results_test.dart @@ -0,0 +1,61 @@ +import 'package:cfnb_app/features/results/result_state.dart'; +import 'package:cfnb_app/features/settings/settings_fields.dart'; +import 'package:flutter_test/flutter_test.dart'; + +void main() { +  group('parseResultLines', () { +    test('parses ip.txt with speed and latency', () { +      const text = '# header\n' +          '1.1.1.1:443#US 120.50 Mbps 30.10 ms\n' +          '2.2.2.2:443#JP 9.80 Mbps 62.10 ms\n'; +      final rows = parseResultLines(text); +      expect(rows.length, 2); +      expect(rows[0].ipPort, '1.1.1.1:443'); +      expect(rows[0].country, 'US'); +      expect(rows[0].speed, '120.50 Mbps'); +      expect(rows[0].latency, '30.10 ms'); +    }); +    test('parses plain node lines (subscription output)', () { +      const text = '1.1.1.1:443#US\n2.2.2.2:443#JP'; +      final rows = parseResultLines(text); +      expect(rows.length, 2); +      expect(rows[0].speed, isNull); +      expect(rows[1].country, 'JP'); +    }); +  }); + +  group('settingsFields', () { +    test('covers live SETTINGS_FIELDS keys', () { +      final keys = settingsFields.map((f) => f.key).toSet(); +      for (final k in const [ +        'SUB_CONVERT_ENABLED', 'SUB_GENERATORS', 'SUB_URLS', +        'SUB_LATENCY_MAX_MS', 'SUB_LATENCY_PROBES', 'SUB_LATENCY_SNI', +        'SUB_QUALITY_LATENCY_WEIGHT', 'SUB_SPEED_ENABLED', 'SUB_SPEED_SIZE_MB', +        'GITHUB_REPO', 'GITHUB_BRANCH', 'GUI_THEME', +      ]) { +        expect(keys.contains(k), isTrue, reason: k); +      } +      // 姝诲瓧娈靛凡绉婚櫎 +      for (final k in const [ +        'PRE_FILTER_PORT_ENABLED', 'TCP_PROBES', 'TEST_AVAILABILITY', +        'BANDWIDTH_WORKERS', 'USE_GLOBAL_MODE', 'GLOBAL_TOP_N', +        'QUALITY_SPEED_WEIGHT', 'CF_ENABLED', 'CF_API_TOKEN', +        'AUTO_SCHEDULE_ENABLED', 'ASN_SOURCES', +      ]) { +        expect(keys.contains(k), isFalse, reason: k); +      } +    }); +    test('includes github + theme sections', () { +      final keys = settingsFields.map((f) => f.key).toSet(); +      expect(keys.contains('GITHUB_REPO'), isTrue); +      expect(keys.contains('GITHUB_BRANCH'), isTrue); +      expect(keys.contains('GUI_THEME'), isTrue); +    }); +    test('normalizeDraft converts sources text to list', () { +      final draft = {'ADDITIONAL_SOURCES': 'https://a.com\nhttps://b.com\n'}; +      final out = normalizeDraft(draft); +      expect(out['ADDITIONAL_SOURCES'], isA<List>()); +      expect((out['ADDITIONAL_SOURCES'] as List).length, 2); +    }); +  }); +}
``
