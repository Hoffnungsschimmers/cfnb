# Review package Task 4

Commits:


Stat:
 lib/core/latency/latency_filter.dart | 255 ++++++++++++++++++++++-------------  test/core/latency/latency_test.dart  | 238 +++++++++++++++++---------------  2 files changed, 291 insertions(+), 202 deletions(-)

## Full diff

``diff
diff --git a/lib/core/latency/latency_filter.dart b/lib/core/latency/latency_filter.dart index cf8cefa..11d2d98 100644 --- a/lib/core/latency/latency_filter.dart +++ b/lib/core/latency/latency_filter.dart @@ -1,94 +1,161 @@ -import 'dart:io'; - -import 'latency_prober.dart'; - -/// 寤惰繜浼橀€夛細瀵硅妭鐐瑰仛骞跺彂寤惰繜娴嬭瘯锛屾寜寤惰繜鎺掑簭淇濈暀鍓?topn锛屽啓涓昏緭鍑?+ 缁撴瀯鍖?JSON銆?-class LatencyFilter { -  /// 杩愯瀹屾暣娴佺▼銆傝繑鍥?(淇濈暀鑺傜偣鍒楄〃, 娴嬭瘯鏁? 杩為€氭暟)銆?-  static Future<(List<String>, int, int)> run({ -    required List<String> nodes, -    required String outputFile, -    required int topN, -    required Duration timeout, -    required int workers, -    Map<String, String>? nodeSource, -    Future<double?> Function(String ip, int port, Duration timeout)? probe, -  }) async { -    final (ordered, tested, connected) = await latencyProbeAll( -      nodes, -      timeout: timeout, -      workers: workers, -      nodeSource: nodeSource, -      probe: probe, -    ); - -    final keptCount = topN < ordered.length ? topN : ordered.length; -    final kept = ordered.take(keptCount).map((r) => r.node).toList(); - -    final ts = DateTime.now().toString().substring(0, 19); -    final out = File(outputFile); -    await out.create(recursive: true); -    final sb = StringBuffer(); -    sb.writeln('# 寤惰繜浼橀€夌粨鏋?@ $ts | 鍏辨祴 $tested 杩為€?$connected 淇濈暀 $keptCount'); -    for (final r in ordered.take(keptCount)) { -      final src = (nodeSource != null && nodeSource.containsKey(r.node)) -          ? '@${nodeSource[r.node]}' -          : ''; -      if (r.latencyMs == null) { -        sb.writeln('${r.node}$src 瓒呮椂'); -      } else { -        sb.writeln('${r.node}$src ${r.latencyMs!.toStringAsFixed(2)} ms'); -      } -    } -    await out.writeAsString(sb.toString()); - -    // 缁撴瀯鍖?JSON -    final records = ordered.take(keptCount).map((r) { -      final ep = parseEndpoint(r.node); -      return { -        'ip': ep?.$1 ?? '', -        'port': ep?.$2 ?? 0, -        'country': nodeCountry(r.node), -        'source': nodeSource != null ? (nodeSource[r.node] ?? '') : '', -        'latency_ms': r.latencyMs == null ? null : (r.latencyMs! * 1000).round() / 1000, -      }; -    }).toList(); - -    final sourceStats = <String, int>{}; -    for (final rec in records) { -      final s = (rec['source'] as String).isEmpty ? '鏈煡' : (rec['source'] as String); -      sourceStats[s] = (sourceStats[s] ?? 0) + 1; -    } - -    final jsonPath = out.parent.path == '.' ? '${out.path}.json' : '${out.path}.json'; -    final jsonFile = File(jsonPath); -    await jsonFile.writeAsString(_jsonEncode({ -      'generated_at': ts, -      'tested': tested, -      'connected': connected, -      'kept': records.length, -      'source_stats': sourceStats, -      'nodes': records, -    })); - -    return (kept, tested, connected); -  } - -  static String _jsonEncode(Object obj) { -    // 绠€鏄?JSON 搴忓垪鍖栵紝閬垮厤寮曞叆 codec 渚濊禆 -    return _encodeValue(obj); -  } -} - -String _encodeValue(Object? v) { -  if (v == null) return 'null'; -  if (v is String) return '"${v.replaceAll('\\', '\\\\').replaceAll('"', '\\"')}"'; -  if (v is num) return v.toString(); -  if (v is bool) return v.toString(); -  if (v is List) return '[${v.map(_encodeValue).join(',')}]'; -  if (v is Map) { -    final entries = v.entries.map((e) => '${_encodeValue(e.key)}:${_encodeValue(e.value)}'); -    return '{${entries.join(',')}}'; -  } -  return '"$v"'; -} +import 'dart:io'; + +import '../speed/speed_prober.dart'; +import 'latency_prober.dart'; + +/// 寤惰繜浼橀€夛細瀵硅妭鐐瑰仛骞跺彂寤惰繜娴嬭瘯锛屼繚鐣欍€屽欢杩?鈮?[latencyMaxMs]銆嶇殑鑺傜偣锛屽啓涓昏緭鍑?+ 缁撴瀯鍖?JSON銆? +/// +/// 鑻ュ紑鍚甫瀹芥祴閫燂紙[speedEnabled]锛夛紝鍒?*浠呭寤惰繜 鈮?[speedLatencyLimitMs] 鐨勮妭鐐?*娴嬪甫瀹斤紝 +/// 闅忓悗鎸?*缁煎悎璐ㄩ噺鍒?*鎺掑簭锛? +///   quality = wLat路(1 鈭?latency/cutoff) + wSpeed路min(speed/refMbps, 1) +/// 鍏朵腑 wLat = [qualityLatencyWeight]锛寃Speed = 1 鈭?wLat銆傚欢杩熸潈閲嶆洿楂橈紝 +/// 鍥犱负寤惰繜鏄敤鎴风洿鎺ユ劅鐭ョ殑鎸囨爣锛屽甫瀹戒粎鍦ㄥ彲鐢ㄦ€у湴鏉夸箣涓婃墠琚湅閲嶃€? +/// 杈撳嚭鎸夎川閲忓垎闄嶅簭锛岃川閲忔渶楂樼殑 IP 鎺掑湪鏈€鍓嶏紙edgetunnel 鏈€浼樺叆鍙ｏ級銆? +class LatencyFilter { +  /// 杩愯瀹屾暣娴佺▼銆傝繑鍥?(淇濈暀鑺傜偣鍒楄〃, 娴嬭瘯鏁? 杩為€氭暟)銆? +  static Future<(List<String>, int, int)> run({ +    required List<String> nodes, +    required String outputFile, +    required int latencyMaxMs, +    required Duration timeout, +    required int workers, +    int probes = 1, +    double minSuccessRate = 1.0, +    bool speedEnabled = false, +    int speedLatencyLimitMs = 200, +    Duration? speedTimeout, +    int speedBytes = 1 * 1024 * 1024, +    int speedWorkers = 20, +    int speedCap = 300, +    int maxKeep = 200, +    double qualityLatencyWeight = 0.6, +    double bandwidthRefMbps = 100.0, +    Map<String, String>? nodeSource, +    String? sni, +    Future<(double?, int)> Function(String ip, int port, Duration timeout, {int probes, String? sni})? probe, +    Future<double?> Function(String ip, int port, Duration timeout, {int bytes})? speedProbe, +    void Function(String)? onLog, +  }) async { +    final (ordered, tested, connected) = await latencyProbeAll( +      nodes, +      timeout: timeout, +      workers: workers, +      probes: probes, +      minSuccessRate: minSuccessRate, +      nodeSource: nodeSource, +      sni: sni, +      probe: probe, +      onLog: onLog, +    ); + +    final connectedResults = ordered.where((r) => r.latencyMs != null).toList(); +    // 淇濈暀鎴锛氫粎淇濈暀寤惰繜 鈮?latencyMaxMs 鐨勮妭鐐癸紙涓嶅啀鎸夋暟閲忔埅鏂級 +    final withinMax = connectedResults +        .where((r) => r.latencyMs! <= latencyMaxMs) +        .toList() +      ..sort((a, b) => a.latencyMs!.compareTo(b.latencyMs!)); +    final capped = withinMax.take(maxKeep).toList(); + +    Map<String, double?> speedMap = {}; +    if (speedEnabled && capped.isNotEmpty) { +      // 浠呭寤惰繜濂界殑鑺傜偣娴嬪甫瀹? +      final good = capped +          .where((r) => r.latencyMs! <= speedLatencyLimitMs) +          .toList(); +      final pool = good.isNotEmpty ? good : capped; +      final bwNodes = pool.take(speedCap < pool.length ? speedCap : pool.length).map((r) => r.node).toList(); +      speedMap = await measureBandwidthAll( +        bwNodes, +        timeout: speedTimeout ?? const Duration(seconds: 15), +        bytes: speedBytes, +        workers: speedWorkers, +        onLog: onLog, +      ); +      if (onLog != null) { +        final ok = speedMap.values.where((v) => v != null).length; +        onLog('甯﹀娴嬮€燂細鍊欓€?${bwNodes.length}锛屾祴寰楅€熷害 $ok'); +      } +    } + +    // 缁煎悎璐ㄩ噺鍒嗭細寤惰繜鏉冮噸 + 甯﹀鏉冮噸锛堝甫瀹芥潈閲?= 1 - 寤惰繜鏉冮噸锛夈€? +    // 寤惰繜褰掍竴涓?1 - latency/cutoff锛?ms鈫?.0锛屾埅姝⑩啋0锛夛紱甯﹀褰掍竴涓?min(speed/ref,1)銆? +    final speedWeight = (1.0 - qualityLatencyWeight).clamp(0.0, 1.0); +    double quality(LatencyResult r) { +      final latScore = ((latencyMaxMs - r.latencyMs!) / latencyMaxMs).clamp(0.0, 1.0); +      final bw = speedMap[r.node]; +      final bwScore = bw == null ? 0.0 : (bw / bandwidthRefMbps).clamp(0.0, 1.0); +      return qualityLatencyWeight * latScore + speedWeight * bwScore; +    } + +    final keptResults = capped.toList() +      ..sort((a, b) => quality(b).compareTo(quality(a))); // 楂樿川閲忓湪鍓? + +    final keptCount = keptResults.length; +    final ts = DateTime.now().toString().substring(0, 19); +    final out = File(outputFile); +    await out.create(recursive: true); +    final sb = StringBuffer(); +    sb.writeln('# 寤惰繜浼橀€夌粨鏋?@ $ts | 鍏辨祴 $tested 杩為€?$connected 淇濈暀 $keptCount'); +    for (final r in keptResults) { +      final src = (nodeSource != null && nodeSource.containsKey(r.node)) +          ? '@${nodeSource[r.node]}' +          : ''; +      final spd = (speedMap[r.node] != null) ? ' ${speedMap[r.node]!.toStringAsFixed(2)}Mbps' : ''; +      final lat = (r.latencyMs != null) ? ' ${r.latencyMs!.toStringAsFixed(2)}ms' : ' 瓒呮椂'; +      final q = quality(r); +      sb.writeln('${r.node}$src$lat$spd Q${q.toStringAsFixed(2)}'); +    } +    await out.writeAsString(sb.toString()); + +    // 缁撴瀯鍖?JSON +    final records = keptResults.map((r) { +      final ep = parseEndpoint(r.node); +      return { +        'ip': ep?.$1 ?? '', +        'port': ep?.$2 ?? 0, +        'country': nodeCountry(r.node), +        'source': nodeSource != null ? (nodeSource[r.node] ?? '') : '', +        'latency_ms': r.latencyMs == null ? null : (r.latencyMs! * 1000).round() / 1000, +        'speed_mbps': speedMap[r.node], +        'quality': (quality(r) * 1000).round() / 1000, +      }; +    }).toList(); + +    final sourceStats = <String, int>{}; +    for (final rec in records) { +      final s = (rec['source'] as String).isEmpty ? '鏈煡' : (rec['source'] as String); +      sourceStats[s] = (sourceStats[s] ?? 0) + 1; +    } + +    final jsonPath = out.parent.path == '.' ? '${out.path}.json' : '${out.path}.json'; +    final jsonFile = File(jsonPath); +    await jsonFile.writeAsString(_jsonEncode({ +      'generated_at': ts, +      'tested': tested, +      'connected': connected, +      'kept': records.length, +      'source_stats': sourceStats, +      'nodes': records, +    })); + +    return (keptResults.map((r) => r.node).toList(), tested, connected); +  } + +  static String _jsonEncode(Object obj) { +    // 绠€鏄?JSON 搴忓垪鍖栵紝閬垮厤寮曞叆 codec 渚濊禆 +    return _encodeValue(obj); +  } +} + +String _encodeValue(Object? v) { +  if (v == null) return 'null'; +  if (v is String) return '"${v.replaceAll('\\', '\\\\').replaceAll('"', '\\"')}"'; +  if (v is num) return v.toString(); +  if (v is bool) return v.toString(); +  if (v is List) return '[${v.map(_encodeValue).join(',')}]'; +  if (v is Map) { +    final entries = v.entries.map((e) => '${_encodeValue(e.key)}:${_encodeValue(e.value)}'); +    return '{${entries.join(',')}}'; +  } +  return '"$v"'; +} diff --git a/test/core/latency/latency_test.dart b/test/core/latency/latency_test.dart index 140973a..c331c6f 100644 --- a/test/core/latency/latency_test.dart +++ b/test/core/latency/latency_test.dart @@ -1,108 +1,130 @@ -import 'dart:io'; - -import 'package:cfnb_app/core/latency/latency_filter.dart'; -import 'package:cfnb_app/core/latency/latency_prober.dart'; -import 'package:flutter_test/flutter_test.dart'; - -void main() { -  group('parseEndpoint', () { -    test('parses ipv4', () { -      final r = parseEndpoint('1.2.3.4:443#US'); -      expect(r, isNotNull); -      expect(r!.$1, '1.2.3.4'); -      expect(r.$2, 443); -    }); -    test('parses ipv6', () { -      final r = parseEndpoint('[2001:db8::1]:443#US'); -      expect(r!.$1, '2001:db8::1'); -      expect(r.$2, 443); -    }); -    test('rejects bad port', () { -      expect(parseEndpoint('1.2.3.4:99999#US'), isNull); -      expect(parseEndpoint('notanode'), isNull); -    }); -  }); - -  group('nodeCountry / nodeSource', () { -    test('extracts country and source', () { -      expect(nodeCountry('1.2.3.4:443#JP@CM'), 'JP'); -      expect(nodeSource('1.2.3.4:443#JP@CM'), 'CM'); -      expect(nodeCountry('1.2.3.4:443'), ''); -      expect(nodeSource('1.2.3.4:443#JP'), ''); -    }); -  }); - -  group('latencyProbeAll', () { -    test('orders succeeded before failed, ascending latency', () async { -      // 娉ㄥ叆鍋囨帰娴嬶細鎸?ip 鏈綅鍐冲畾寤惰繜锛岀壒瀹?ip 瓒呮椂 -      Future<double?> fake(String ip, int port, Duration _) async { -        if (ip.endsWith('.9')) return null; // 瓒呮椂 -        final last = int.parse(ip.split('.').last); -        return last.toDouble(); -      } -      final nodes = [ -        '10.0.0.3:443#US', -        '10.0.0.1:443#US', -        '10.0.0.9:443#US', // 澶辫触 -      ]; -      final (ordered, tested, connected) = await latencyProbeAll( -        nodes, -        timeout: const Duration(seconds: 2), -        workers: 5, -        probe: fake, -      ); -      expect(tested, 3); -      expect(connected, 2); -      expect(ordered.first.latencyMs, 1.0); -      expect(ordered.last.latencyMs, isNull); -    }); - -    test('respects worker semaphore (no overflow)', () async { -      var concurrent = 0; -      var maxConcurrent = 0; -      Future<double?> fake(String ip, int port, Duration _) async { -        concurrent++; -        maxConcurrent = maxConcurrent < concurrent ? concurrent : maxConcurrent; -        await Future.delayed(const Duration(milliseconds: 10)); -        concurrent--; -        return 1.0; -      } -      final nodes = List.generate(20, (i) => '10.0.0.$i:443#US'); -      await latencyProbeAll(nodes, timeout: const Duration(seconds: 2), workers: 4, probe: fake); -      expect(maxConcurrent, lessThanOrEqualTo(4)); -    }); -  }); - -  group('LatencyFilter.run', () { -    test('writes output and json with topn', () async { -      Future<double?> fake(String ip, int port, Duration _) async { -        if (ip.endsWith('.9')) return null; -        return int.parse(ip.split('.').last).toDouble(); -      } -      final dir = await Directory.systemTemp.createTemp('cfnb_lat_'); -      final out = '${dir.path}/addressesapi_top.txt'; -      final nodes = [ -        '10.0.0.5:443#US@CM', -        '10.0.0.2:443#US@CM', -        '10.0.0.9:443#US@CM', -      ]; -      final (kept, tested, connected) = await LatencyFilter.run( -        nodes: nodes, -        outputFile: out, -        topN: 2, -        timeout: const Duration(seconds: 2), -        workers: 5, -        nodeSource: {'10.0.0.5:443#US@CM': 'CM'}, -        probe: fake, -      ); -      expect(tested, 3); -      expect(connected, 2); -      expect(kept.length, 2); -      final content = await File(out).readAsString(); -      expect(content, contains('ms')); -      final json = await File('$out.json').readAsString(); -      expect(json, contains('"connected":2')); -      await dir.delete(recursive: true); -    }); -  }); -} +import 'dart:io'; + +import 'package:cfnb_app/core/latency/latency_filter.dart'; +import 'package:cfnb_app/core/latency/latency_prober.dart'; +import 'package:flutter_test/flutter_test.dart'; + +void main() { +  group('parseEndpoint', () { +    test('parses ipv4', () { +      final r = parseEndpoint('1.2.3.4:443#US'); +      expect(r, isNotNull); +      expect(r!.$1, '1.2.3.4'); +      expect(r.$2, 443); +    }); +    test('parses ipv6', () { +      final r = parseEndpoint('[2001:db8::1]:443#US'); +      expect(r!.$1, '2001:db8::1'); +      expect(r.$2, 443); +    }); +    test('rejects bad port', () { +      expect(parseEndpoint('1.2.3.4:99999#US'), isNull); +      expect(parseEndpoint('notanode'), isNull); +    }); +  }); + +  group('nodeCountry / nodeSource', () { +    test('extracts country and source', () { +      expect(nodeCountry('1.2.3.4:443#JP@CM'), 'JP'); +      expect(nodeSource('1.2.3.4:443#JP@CM'), 'CM'); +      expect(nodeCountry('1.2.3.4:443'), ''); +      expect(nodeSource('1.2.3.4:443#JP'), ''); +    }); +  }); + +  group('latencyProbeAll', () { +    test('orders succeeded before failed, ascending latency', () async { +      // 娉ㄥ叆鍋囨帰娴嬶細鎸?ip 鏈綅鍐冲畾寤惰繜锛岀壒瀹?ip 瓒呮椂 +      Future<(double?, int)> fake(String ip, int port, Duration _, {int probes = 1, String? sni}) async { +        if (ip.endsWith('.9')) return (null, 0); // 瓒呮椂 +        final last = int.parse(ip.split('.').last); +        return (last.toDouble(), 1); +      } +      final nodes = [ +        '10.0.0.3:443#US', +        '10.0.0.1:443#US', +        '10.0.0.9:443#US', // 澶辫触 +      ]; +      final (ordered, tested, connected) = await latencyProbeAll( +        nodes, +        timeout: const Duration(seconds: 2), +        workers: 5, +        probe: fake, +      ); +      expect(tested, 3); +      expect(connected, 2); +      expect(ordered.first.latencyMs, 1.0); +      expect(ordered.last.latencyMs, isNull); +    }); + +    test('respects worker semaphore (no overflow)', () async { +      var concurrent = 0; +      var maxConcurrent = 0; +      Future<(double?, int)> fake(String ip, int port, Duration _, {int probes = 1, String? sni}) async { +        concurrent++; +        maxConcurrent = maxConcurrent < concurrent ? concurrent : maxConcurrent; +        await Future.delayed(const Duration(milliseconds: 10)); +        concurrent--; +        return (1.0, 1); +      } +      final nodes = List.generate(20, (i) => '10.0.0.$i:443#US'); +      await latencyProbeAll(nodes, timeout: const Duration(seconds: 2), workers: 4, probe: fake); +      expect(maxConcurrent, lessThanOrEqualTo(4)); +    }); +  }); + +  group('LatencyFilter.run', () { +    test('writes output and json with latency cutoff', () async { +      Future<(double?, int)> fake(String ip, int port, Duration _, {int probes = 1, String? sni}) async { +        if (ip.endsWith('.9')) return (null, 0); +        return (int.parse(ip.split('.').last).toDouble(), 1); +      } +      final dir = await Directory.systemTemp.createTemp('cfnb_lat_'); +      final out = '${dir.path}/addressesapi_top.txt'; +      final nodes = [ +        '10.0.0.5:443#US@CM', +        '10.0.0.2:443#US@CM', +        '10.0.0.9:443#US@CM', +      ]; +      final (kept, tested, connected) = await LatencyFilter.run( +        nodes: nodes, +        outputFile: out, +        latencyMaxMs: 1000, +        timeout: const Duration(seconds: 2), +        workers: 5, +        nodeSource: {'10.0.0.5:443#US@CM': 'CM'}, +        probe: fake, +      ); +      expect(tested, 3); +      expect(connected, 2); +      expect(kept.length, 2); +      final content = await File(out).readAsString(); +      expect(content, contains('ms')); +      final json = await File('$out.json').readAsString(); +      expect(json, contains('"connected":2')); +      await dir.delete(recursive: true); +    }); + +    test('respects maxKeep cap', () async { +      Future<(double?, int)> fake(String ip, int port, Duration _, {int probes = 1, String? sni}) async { +        return (int.parse(ip.split('.').last).toDouble(), 1); +      } +      final dir = await Directory.systemTemp.createTemp('cfnb_lat_'); +      final out = '${dir.path}/top.txt'; +      final nodes = List.generate(50, (i) => '10.0.0.$i:443#US'); +      final (kept, tested, connected) = await LatencyFilter.run( +        nodes: nodes, +        outputFile: out, +        latencyMaxMs: 1000, +        timeout: const Duration(seconds: 2), +        workers: 5, +        maxKeep: 10, +        probe: fake, +      ); +      expect(tested, 50); +      expect(connected, 50); +      expect(kept.length, 10); +      await dir.delete(recursive: true); +    }); +  }); +}
``
