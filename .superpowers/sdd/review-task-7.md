# Review package Task 7

Commits:


Stat:
 lib/core/github/github_push.dart                   | 102 ++++++++--  .../subscriptions/subscriptions_state.dart         | 213 +++++++++++++++++++++  test/core/github/github_push_test.dart             |   8 +  3 files changed, 305 insertions(+), 18 deletions(-)

## Full diff

``diff
diff --git a/lib/core/github/github_push.dart b/lib/core/github/github_push.dart index 0bc823b..82fd10b 100644 --- a/lib/core/github/github_push.dart +++ b/lib/core/github/github_push.dart @@ -1,41 +1,92 @@  import 'dart:convert'; +import 'dart:io';    import 'package:dio/dio.dart'; +import 'package:dio/src/adapters/io_adapter.dart';    /// GitHub 鏂囦欢鎺ㄩ€侊紙瀵瑰簲鏃х増 scripts/git_sync.ps1 鐨?ip 鏁版嵁鎺ㄩ€侊級銆? ///  /// 鎺ㄩ€佸埌鐙珛鐨?cf-ip 浠撳簱锛堜笌浠ｇ爜浠撳簱闅旂锛夈€備娇鐢?GitHub Contents API锛? /// 鑷姩澶勭悊宸插瓨鍦ㄦ枃浠剁殑 sha锛堟洿鏂帮級鎴栨柊寤恒€倀oken 閫氳繃鍙傛暟浼犲叆锛屼笉钀界洏鏄庢枃銆? class GithubPush {    final String token;    final String repo; // 褰㈠ "owner/cf-ip"    final String branch;    final Dio dio;      /// 鍙€夛細鑷畾涔夎姹傚彂閫佸櫒锛屼究浜庢祴璇曟敞鍏ュ亣缃戠粶灞傘€?   final Future<Response> Function(RequestOptions)? sender;   +  /// 鍏变韩鐩磋繛 Dio锛氫笉瑕嗙洊 findProxy锛屽洜姝よ蛋绯荤粺浠ｇ悊锛團lutter 榛樿锛夛紝 +  /// 閫傜敤浜庤闃呮姄鍙栫瓑闇€缁忔湰鍦颁唬鐞嗗彲杈炬簮鐨勮姹傘€?+  static Dio directDio() => Dio(BaseOptions( +        headers: {'User-Agent': 'cfnb-app'}, +        connectTimeout: const Duration(seconds: 10), +        sendTimeout: const Duration(seconds: 20), +        receiveTimeout: const Duration(seconds: 20), +      )); + +  /// 浠呭厑璁告帹閫佷紭閫夌粨鏋滄枃浠讹紙鏂囦欢鍚嶄互 _top.txt 缁撳熬锛屽 addressesapi_top.txt锛夛紝鍏朵綑鏂囦欢涓嶆帹閫併€?+  static bool isPushable(String file) => +      file.toLowerCase().endsWith('_top.txt'); +    GithubPush({      required this.token,      required this.repo,      this.branch = 'main',      Dio? dio,      this.sender,    }) : dio = dio ?? -            Dio(BaseOptions( -              baseUrl: 'https://api.github.com', -              headers: { -                'Authorization': 'Bearer $token', -                'Accept': 'application/vnd.github+json', -              }, -            )); +        Dio(BaseOptions( +          baseUrl: 'https://api.github.com', +          validateStatus: (_) => true, +          connectTimeout: const Duration(seconds: 15), +          receiveTimeout: const Duration(seconds: 20), +          headers: { +            'Authorization': 'Bearer ${token.trim()}', +            'Accept': 'application/vnd.github+json', +            'User-Agent': 'cfnb-app', +          }, +        )) { +    // GitHub API 璧扮洿杩烇紝缁曞紑鏈湴浠ｇ悊锛堝 127.0.0.1:7890 鐨?Clash锛夆€斺€?+    // 缁忎唬鐞嗚闂?api.github.com 浼氳闃绘柇/涓㈠純锛堝疄娴?HTTP 000锛夛紝鐩磋繛鍒欐甯搞€?+    final adapter = this.dio.httpClientAdapter; +    if (adapter is IOHttpClientAdapter) { +      adapter.createHttpClient = () { +        final client = HttpClient(); +        client.findProxy = (uri) => 'DIRECT'; +        client.userAgent = 'cfnb-app'; +        return client; +      }; +    } +  } + +  /// 鏋勯€犲崟娆¤姹傦細鎶?Token / UA 鏄惧紡甯﹀埌 RequestOptions锛岄伩鍏嶆煇浜?Dio 鐗堟湰 +  /// 鍦?`fetch()` 鏃朵笉缁ф壙 BaseOptions.headers 瀵艰嚧 401銆?+  RequestOptions _req(String method, String path, {Map<String, dynamic>? data}) { +    final headers = <String, dynamic>{ +      'Authorization': 'Bearer ${token.trim()}', +      'Accept': 'application/vnd.github+json', +      'User-Agent': 'cfnb-app', +    }; +    return RequestOptions( +      method: method, +      path: path, +      baseUrl: dio.options.baseUrl, +      headers: headers, +      data: data, +      validateStatus: dio.options.validateStatus, +      connectTimeout: dio.options.connectTimeout, +      receiveTimeout: dio.options.receiveTimeout, +    ); +  }      Future<Response> _send(RequestOptions options) => sender != null        ? sender!(options)        : dio.fetch(options);      /// 鏋勯€?PUT body锛堟彁鍙栧凡鏈?sha 鐢ㄤ簬鏇存柊锛屾垨浠呮柊寤猴級銆?   static Map<String, dynamic> buildPutBody({      required String path,      required String content,      required String branch, @@ -44,39 +95,54 @@ class GithubPush {    }) =>        {          'message': message ?? 'update $path',          'content': base64Encode(utf8.encode(content)),          'branch': branch,          if (sha != null) 'sha': sha,        };      /// 鎺ㄩ€佸崟涓枃浠跺唴瀹瑰埌浠撳簱锛岃繑鍥?HTTP 鐘舵€佺爜銆?   Future<int> pushFile(String path, String content, {String? message}) async { +    // Token 鏈夋晥鎬ц嚜妫€锛?01 绔嬪嵆缁欏嚭鏄庣‘鎻愮ず锛岄伩鍏嶇湅 GitHub 鍘熷鎶ラ敊銆?+    try { +      final who = await _send(_req('GET', '/user')); +      if (who.statusCode == 401) { +        throw Exception( +            'GitHub Token 鏃犳晥锛圙itHub 杩斿洖 401锛夈€傝锛氣憼 鍦?GitHub 缃戦〉閲嶆柊鐢熸垚 Classic token 骞跺彧鍕?repo锛涒憽 澶嶅埗鏃跺厛娓呯┖鍐嶆暣娈电矘璐?\$token锛岄伩鍏嶅甫鍏ョ┖鏍?鎹㈣/鍏ㄨ绌烘牸'); +      } +    } on DioException { +      // /user 寮傚父涔熸寜鏃犳晥澶勭悊锛岀敱涓嬫柟 PUT 缁欏嚭鏈€缁堥敊璇?+    } +      final url = '/repos/$repo/contents/$path';      String? sha;      try { -      final existing = await _send(RequestOptions( -        method: 'GET', -        path: url, -        baseUrl: dio.options.baseUrl, -      )); +      final existing = await _send(_req('GET', url));        sha = (existing.data is Map ? existing.data['sha'] as String? : null);      } on DioException {        // 鏂囦欢涓嶅瓨鍦紝鏂板缓      }        final body = buildPutBody(        path: path,        content: content,        branch: branch,        message: message,        sha: sha,      ); -    final resp = await _send(RequestOptions( -      method: 'PUT', -      path: url, -      baseUrl: dio.options.baseUrl, -      data: body, -    )); +    final resp = await _send(_req('PUT', url, data: body)); +    if (resp.statusCode == 401) { +      throw Exception( +          'GitHub 401锛歍oken 鏃犳晥鎴栨棤璇ヤ粨搴撹闂潈銆傝妫€鏌ワ細鈶?Token 鏄惁瀹屾暣鏃犵┖鏍?鎹㈣锛涒憽 Repo 鏄惁鎷煎啓涓恒€宱wner/浠撳悕銆嶄笖 Token 鏈?repo 鏉冮檺锛涒憿 璇ヤ粨纭疄瀛樺湪'); +    }      return resp.statusCode ?? 0;    } + +  /// 鎵归噺鎺ㄩ€佸涓枃浠讹紝杩斿洖 璺緞 -> HTTP 鐘舵€佺爜 鐨勬槧灏勩€?+  Future<Map<String, int>> pushMultiple(Map<String, String> files, {String? message}) async { +    final results = <String, int>{}; +    for (final entry in files.entries) { +      results[entry.key] = await pushFile(entry.key, entry.value, message: message); +    } +    return results; +  }  } diff --git a/lib/features/subscriptions/subscriptions_state.dart b/lib/features/subscriptions/subscriptions_state.dart new file mode 100644 index 0000000..093b6a9 --- /dev/null +++ b/lib/features/subscriptions/subscriptions_state.dart @@ -0,0 +1,213 @@ +import 'dart:io'; + +import 'package:dio/dio.dart' as dio_pkg; +import 'package:flutter_riverpod/flutter_riverpod.dart'; + +import '../../app/providers.dart'; +import '../../core/config/app_config.dart'; +import '../../core/github/github_push.dart'; +import '../../core/latency/latency_filter.dart'; +import '../../core/latency/latency_prober.dart'; +import '../../core/subscription/subscription_converter.dart'; +import '../results/result_state.dart'; + +/// 璁㈤槄鍣ㄧ姸鎬侊細浠呬繚鐣欍€岃闃匢P銆嶄笌銆屽欢杩熶紭閫夈€嶄袱涓姩浣滐紝鍚繍琛屼腑鏍囪銆?+class SubscriptionsState { +  final bool running; +  SubscriptionsState({this.running = false}); +  SubscriptionsState copyWith({bool? running}) => +      SubscriptionsState(running: running ?? this.running); +} + +class SubscriptionsNotifier extends StateNotifier<SubscriptionsState> { +  final Ref ref; +  SubscriptionsNotifier(this.ref) : super(SubscriptionsState()); + +  /// 鍗曠嫭锛氳闃匢P锛堣浆鎹㈣闃呭櫒 -> addressesapi.txt锛夈€?+  Future<void> runSubscription() async { +    if (state.running) return; +    final cfg = await _cfg(); +    final parser = await ref.read(nodeParserProvider.future); +    final logger = ref.read(subLoggerProvider); +    state = state.copyWith(running: true); +    logger.info('寮€濮嬨€岃闃匢P銆嶈浆鎹?); +    try { +      if (!cfg.subConvertEnabled) { +        logger.info('璁㈤槄杞崲鏈惎鐢紙subConvertEnabled=false锛?); +        return; +      } +      final (nodes, srcMap) = await convertSubscriptions( +        cfg, +        fetch: (url) => _safeFetch(url), +        resolve: _resolveHost, +        parser: parser, +        onLog: (m) => logger.info(m), +      ); +      if (nodes.isEmpty) { +        logger.info('璁㈤槄杞崲鏃犲彲鐢ㄨ妭鐐癸紙璇锋鏌?subGenerators/subUrls 閰嶇疆锛?); +      } else { +        await writeSubOutput(nodes, cfg.subOutputFile); +        await writeSourceMap(srcMap, cfg.subOutputFile); +        await ref.read(resultProvider.notifier).loadFile(cfg.subOutputFile); +        logger.info('璁㈤槄IP杞崲瀹屾垚锛?{nodes.length} 涓妭鐐?-> ${cfg.subOutputFile}'); +      } +    } catch (e) { +      logger.error(e.toString()); +    } finally { +      state = state.copyWith(running: false); +    } +  } + +  /// 鍗曠嫭锛氬欢杩熶紭閫夛紙瀵硅闃匢P鍋氬欢杩熸祴璇曪紝淇濈暀鍓?N 鍚?-> addressesapi_top.txt锛夈€?+  Future<void> runLatency() async { +    if (state.running) return; +    final cfg = await _cfg(); +    final logger = ref.read(subLoggerProvider); +    state = state.copyWith(running: true); +    logger.info('寮€濮嬨€屽欢杩熶紭閫夈€?); +    try { +      final nodes = await _readNodes(cfg.subOutputFile); +      if (nodes.isEmpty) { +        logger.info('鏈壘鍒拌闃匢P鏂囦欢锛?{cfg.subOutputFile}锛夛紝璇峰厛杩愯銆岃闃匢P銆?); +      } else { +        final (kept, tested, connected) = await LatencyFilter.run( +          nodes: nodes, +          outputFile: cfg.subLatencyOutputFile, +          latencyMaxMs: cfg.subLatencyMaxMs, +          timeout: Duration(milliseconds: (cfg.subLatencyTimeout * 1000).round()), +          workers: cfg.subLatencyWorkers, +          probes: cfg.subLatencyProbes, +          minSuccessRate: 1.0, +          speedEnabled: cfg.subSpeedEnabled, +          speedLatencyLimitMs: cfg.subSpeedLatencyLimit, +          qualityLatencyWeight: cfg.subQualityLatencyWeight, +          speedTimeout: Duration(milliseconds: (cfg.subSpeedTimeout * 1000).round()), +          speedBytes: (cfg.subSpeedSizeMb * 1024 * 1024).round(), +          speedWorkers: cfg.subSpeedWorkers, +          probe: measureLatency, +          onLog: (m) => logger.info(m), +        ); +        logger.info('寤惰繜浼橀€夊畬鎴愶細娴嬭瘯 $tested / 杩為€?$connected / 淇濈暀 ${kept.length}'); +        await ref.read(resultProvider.notifier).loadFile(cfg.subLatencyOutputFile); +      } +    } catch (e) { +      logger.error(e.toString()); +    } finally { +      state = state.copyWith(running: false); +    } +  } + +  Future<AppConfig> _cfg() async { +    final repo = await ref.read(configRepositoryProvider.future); +    return repo.current; +  } + +  GithubPush? _github(AppConfig cfg) => +      cfg.githubToken.isEmpty ? null : GithubPush(token: cfg.githubToken, repo: cfg.githubRepo, branch: cfg.githubBranch); + +  /// 鎵嬪姩鎺ㄩ€佸崟涓骇鐗╂枃浠跺埌 GitHub锛坈f-ip 浠擄級銆傝繑鍥?(鏄惁鎴愬姛, HTTP鐮? 娑堟伅)銆?+  /// 鏈厤缃?Token / 鏂囦欢涓嶅瓨鍦ㄦ椂杩斿洖澶辫触鍘熷洜锛屼笉鎶涘紓甯搞€?+  Future<(bool, int, String)> pushFile(String file) async { +    if (!GithubPush.isPushable(file)) { +      final m = '浠呮敮鎸佹帹閫佸悗缂€涓?_top.txt 鐨勪紭閫夌粨鏋滄枃浠讹紙褰撳墠锛?file锛?; +      ref.read(subLoggerProvider).info(m); +      return (false, 0, m); +    } +    final cfg = await _cfg(); +    final logger = ref.read(subLoggerProvider); +    final github = _github(cfg); +    if (github == null) { +      final m = 'GitHub 鏈厤缃紙璇峰湪璁剧疆濉啓 Token/Repo/Branch锛夛紝璺宠繃鎺ㄩ€侊細$file'; +      logger.info(m); +      return (false, 0, m); +    } +    if (!File(file).existsSync()) { +      final m = '鏂囦欢涓嶅瓨鍦細$file'; +      logger.error(m); +      return (false, 0, m); +    } +    try { +      final content = await File(file).readAsString(); +      logger.info('寮€濮嬫帹閫?$file 鍒?GitHub锛?{cfg.githubRepo}@${cfg.githubBranch}锛夆€?); +      final code = await github.pushFile(file, content, message: 'update $file'); +      logger.info('宸叉帹閫?$file (HTTP $code)'); +      return (true, code, '宸叉帹閫?$file (HTTP $code)'); +    } catch (e) { +      final m = '鎺ㄩ€?$file 澶辫触锛?e'; +      logger.error(m); +      return (false, 0, m); +    } +  } + +  // ---- 缃戠粶杈呭姪 ---- +  // 澶嶇敤 GithubPush 鐨勭洿杩?Dio锛堣蛋绯荤粺浠ｇ悊锛岄€傜敤浜庤闃呮簮鎶撳彇锛夈€?+  final _dio = GithubPush.directDio(); + +  Future<String> _httpFetch(String url, {Duration? connectTimeout}) async { +    final resp = await _dio.get(url, +        options: dio_pkg.Options( +          responseType: dio_pkg.ResponseType.plain, +          headers: { +            'User-Agent': edgetunnelUa, +            'Accept': '*/*', +          }, +          connectTimeout: connectTimeout ?? const Duration(seconds: 10), +          sendTimeout: const Duration(seconds: 20), +          receiveTimeout: const Duration(seconds: 20), +        )); +    return resp.data.toString(); +  } + +  /// 瀹归敊鎶撳彇锛氬崟娆″皾璇曪紝澶辫触鍙褰曚竴娆″苟璺宠繃锛屼笉閲嶈瘯銆佷笉涓柇鏁翠綋杞崲銆?+  /// 锛堟婧?TLS 鎻℃墜澶辫触灞炴簮鑷韩闂锛岄噸璇曟棤鎰忎箟锛涘鍊欓€?URL 鐨勫洖閫€鍦?convertSubscriptions 鍐呭鐞嗐€傦級 +  Future<String> _safeFetch(String url) async { +    final logger = ref.read(subLoggerProvider); +    try { +      return await _httpFetch(url); +    } catch (e) { +      final msg = e.toString().replaceAll(RegExp(r'\n'), ' '); +      // edgetunnel 绯昏闃呭櫒杩斿洖 403锛岄€氬父鏄閮ㄧ讲鏈惎鐢?BEST_SUB锛?+      // 闇€鍦?Cloudflare Workers 鍙橀噺涓缃?BEST_SUB=1锛堟垨 true锛夛紝鏈伐鍏风殑 +      // host=example.com / uuid=00000000... / edgetunnel UA 鎵嶄細琚瘑鍒负浼橀€夎闃呯敓鎴愬櫒銆?+      final hint = (msg.contains('403') && url.contains('/sub')) +          ? '锛堣 edgetunnel 閮ㄧ讲鍙兘鏈惎鐢?BEST_SUB锛氳鍦?Cloudflare 鍙橀噺璁剧疆 BEST_SUB=1锛? +          : ''; +      logger.error('鎶撳彇澶辫触 [$url]锛?msg$hint'); +      return ''; +    } +  } + +  Future<String?> _resolveHost(String host) async { +    if (_isIp(host)) return host; +    try { +      final list = await InternetAddress.lookup(host); +      return list.isNotEmpty ? list.first.address : null; +    } on Object { +      return null; +    } +  } + +  bool _isIp(String host) { +    if (host.contains(':')) { +      final h = host.replaceAll(RegExp(r'[\[\]]'), ''); +      return h.contains(RegExp(r'^[0-9a-fA-F:]+$')); +    } +    final parts = host.split('.'); +    if (parts.length != 4) return false; +    return parts.every((p) => int.tryParse(p) != null); +  } + +  Future<List<String>> _readNodes(String path) async { +    final f = File(path); +    if (!f.existsSync()) return []; +    final text = await f.readAsString(); +    return text +        .split('\n') +        .map((e) => e.trim()) +        .where((e) => e.isNotEmpty && !e.startsWith('#')) +        .toList(); +  } +} + +final subProvider = StateNotifierProvider<SubscriptionsNotifier, SubscriptionsState>( +    (ref) => SubscriptionsNotifier(ref)); diff --git a/test/core/github/github_push_test.dart b/test/core/github/github_push_test.dart index e53751f..e070148 100644 --- a/test/core/github/github_push_test.dart +++ b/test/core/github/github_push_test.dart @@ -44,11 +44,19 @@ void main() {          }          final data = o.data as Map;          expect(data['sha'], 'SHA1');          return Response(statusCode: 200, requestOptions: o, data: {});        }        final gh = GithubPush(token: 't', repo: 'o/cf-ip', sender: fakeSender);        final code = await gh.pushFile('ip.txt', 'hello');        expect(code, 200);      });    }); + +  group('isPushable', () { +    test('only .top files', () { +      expect(GithubPush.isPushable('addressesapi_top.txt'), isTrue); +      expect(GithubPush.isPushable('addressesapi.txt'), isFalse); +      expect(GithubPush.isPushable('ip.txt'), isFalse); +    }); +  });  }
``
